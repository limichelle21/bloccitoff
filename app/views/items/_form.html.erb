
<%= form_for [user, item] do |f| %>

	<div class="form-group">
		<%= f.label :body %>
		<%= f.text_field :body, rows: 2, class: 'form-control', placeholder: "Enter to-do list item." %>
		<%= f.submit "Save Item", class: 'btn btn-success' %>
	</div>

<% end %>